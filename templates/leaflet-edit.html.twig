{#
/**
 * @file
 * Default theme implementation for the editable Leaflet map.
 *
 * @ingroup themeable
 */
#}

<div id="{{ map_id }}" style="min-width: 150px; {% if height is not empty %}height: {{ height }}{% endif %}"></div>
        